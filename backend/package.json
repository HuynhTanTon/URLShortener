{
  "name": "url-shortener-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Logic Lambda cho URL Shortener - dung chung cho local (Docker) va AWS Lambda thuc te",
  "main": "local-server.mjs",
  "scripts": {
    "start": "node local-server.mjs"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.600.0"
  }
}
